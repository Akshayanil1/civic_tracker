[build-system]
requires = ["setuptools>=62.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 120
select = ["E", "F", "I", "W"]
ignore = ["E101", "W191"]

[tool.ruff.isort]
known-first-party = ["civic_tracker"]
